Extract physics problem from image and return ONLY this JSON structure:
{
  "problem_text": "string",
  "knowns": {"variable": "value with units"},
  "unknowns": ["variable"],
  "diagram_description": "string",
  "suggested_scenario": "kinematics_1d|inclined_plane|projectile_motion|atwood_machine|collision_1d|rotational_kinematics|mass_spring"
}

CRITICAL - ANGLE EXTRACTION FROM DIAGRAMS:
You MUST carefully examine the diagram for ANY angle indication:
- Look for angle symbols: θ (theta), φ (phi), α (alpha), β (beta)
- Look for degree symbols: ° (e.g., "10°", "30°", "45°")
- Look for angle labels near inclines/ramps/slopes
- Look for right angle markers (□)
- If the diagram shows an inclined plane, ramp, slope, or wedge, there IS an angle - find it!
- If visible, include in knowns as "diagram_angle": "X deg" (e.g., "diagram_angle": "10 deg")
- NOTE: The diagram angle may not match the physical angle derived from kinematics. The reasoning model will compute the physical angle from a = 2d/t² and θ = arcsin(a/g).

SCENARIO CLASSIFICATION RULE (HIGHEST PRIORITY):
- If problem gives distance (d) and time (t) with "initially at rest" (or v0=0), ALWAYS classify as "kinematics_1d" (constant acceleration from kinematic equations: a = 2d/t²)
- Only classify as "inclined_plane" if angle and mass are given WITHOUT distance/time data
- **IMPORTANT**: Even for kinematics_1d, if diagram shows an incline, you MUST still extract the angle for 3D visualization

**CONCEPTUAL/MULTIPLE-CHOICE DETECTION (HIGHEST PRIORITY):**
- If question has multiple choice options (A, B, C, D or similar letters)
- If question asks "which expression", "which formula", "which equation", "which of the following"
- If question asks for a SYMBOLIC answer (e.g., "v0 sin θ", "2 cos 35°") not a number
- If question can be answered WITHOUT numerical computation
→ classify as "conceptual_mc" (multiple choice conceptual)
→ knowns: include any given values
→ unknowns: the conceptual question being asked
→ diagram_description: describe the diagram if relevant

**MASS-SPRING DETECTION RULES:**
- If problem mentions "mass on a spring", "spring constant k", "oscillat", "SHM", "simple harmonic"
- If diagram shows a vertical/horizontal spring with attached mass
- Look for: spring constant (k), mass (m), initial displacement (x₀ or A), initial velocity (v₀)
- For vertical springs: equilibrium extension x_eq = mg/k may be shown

**ROTATIONAL KINEMATICS DETECTION RULES:**
- If problem mentions "rotates", "rotating", "spinning", "angular", "wheel", "disk", "flywheel", "pulley spins", "grindstone", "turntable", "rpm", "revolutions", "revolves", "turns through"
- If diagram shows a rotating object (circle with axis, wheel, disk, pulley) with angle, angular velocity, or axis labels
- Look for: angular velocity (ω or "rpm"), initial angular velocity (ω₀), angle (θ, "revolutions", or "turns"), angular acceleration (α), time (t), radius (r), torque (τ), moment of inertia (I)
- UNITS: ALWAYS convert into SI when writing knowns:
  - "300 rpm" → "omega0": "31.42 rad/s" (convert: value * 2*pi/60 = value * 0.10472)
  - "X revolutions"/"X rev"/"X turns" → radians (convert: X * 2*pi)
  - Keep the ORIGINAL units in a comment-free knowns entry using the raw key (e.g., "omega": "300 rpm") so nothing is lost
- If only mass + radius (and no spring/incline), and the problem involves torque or angular acceleration → rotational_kinematics
- NOTE: plain circular/centripetal motion problems (v²/r, orbits) WITHOUT angular quantities (ω, θ, α, rpm, revolutions) are NOT rotational_kinematics in this app's scope

Scenario types:
- projectile_motion: Object launched at angle, parabolic trajectory under gravity
- kinematics_1d: Straight-line motion with constant acceleration (including on inclines if d+t given)
- inclined_plane: Block on ramp with friction, forces decomposition (when NO d/t given)
- atwood_machine: Two masses connected by string over pulley
- collision_1d: Two objects colliding in one dimension
- rotational_kinematics: Rotating object with angular motion
- mass_spring: Mass on spring oscillating
- energy_conservation: Roller coaster, pendulum, spring - energy transforms
- conceptual_mc: Multiple choice conceptual question (which expression/formula/graph)

EXTRACTION RULES:
- "initially at rest" or "starts from rest" → v0 = 0 m/s
- "covers a distance of X m" → distance = X m (or d = X m)
- "in Y s" or "time of Y s" → time = Y s (or t = Y s)
- "mass m" or "mass of m kg" → mass = m (use numeric value if given, otherwise "m")
- Include ALL numeric values with units in knowns
- Unknowns should be the variable NAME only (e.g., "a" for acceleration, "v" for speed)

FEW-SHOT EXAMPLES:
Example 1: "A car of mass m descending a slope. The car, initially at rest, covers a distance of 27 m in 3.0 s. Diagram shows a 10° incline."
→ suggested_scenario: "kinematics_1d" (distance + time + v0=0 given)
→ knowns: {"distance": "27 m", "time": "3.0 s", "v0": "0 m/s", "diagram_angle": "10 deg"} (include diagram angle for 3D rendering!)

Example 2: "A block of mass 5 kg on a 30° incline with μ_k = 0.2. Find the acceleration."
→ suggested_scenario: "inclined_plane" (angle + mass given, NO distance/time)
→ knowns: {"mass": "5 kg", "angle": "30 deg", "mu_k": "0.2"}

Example 3: Diagram shows a block on a ramp labeled "θ = 25°". Text: "A 2 kg block slides down a frictionless incline. Find acceleration."
→ suggested_scenario: "inclined_plane"
→ knowns: {"mass": "2 kg", "angle": "25 deg"}

Example 4: "A 0.5 kg mass attached to a spring (k = 200 N/m) is displaced 0.1 m from equilibrium and released from rest. Find the period and maximum speed."
→ suggested_scenario: "mass_spring"
→ knowns: {"mass": "0.5 kg", "k": "200 N/m", "x0": "0.1 m", "v0": "0 m/s"}
→ unknowns: ["period", "max_speed", "omega"]

Example 5: Diagram shows a vertical spring with mass m = 2 kg, spring constant k = 50 N/m. The mass is pulled down 0.15 m from equilibrium and released.
→ suggested_scenario: "mass_spring"
→ knowns: {"mass": "2 kg", "k": "50 N/m", "x0": "0.15 m", "v0": "0 m/s"}
→ unknowns: ["period", "frequency", "max_acceleration"]

Example 6: Diagram shows a vertical spring with mass m = 0.1 kg, spring constant k = 40 N/m, g = 10 m/s². The block is attached to an UNSTRETCHED spring and released from rest.
→ suggested_scenario: "mass_spring"
→ knowns: {"mass": "0.1 kg", "k": "40 N/m", "g": "10 m/s^2", "v0": "0 m/s"}
→ unknowns: ["amplitude", "period", "omega"]
→ diagram_description: "Vertical spring initially unstretched, block attached and released"

Example 7: "A wheel initially at rest rotates with constant angular acceleration 2.0 rad/s² for 5.0 s. Find the final angular velocity and the total angle turned."
→ suggested_scenario: "rotational_kinematics"
→ knowns: {"omega0": "0 rad/s", "alpha": "2.0 rad/s^2", "time": "5.0 s"}
→ unknowns: ["omega", "theta"]
→ diagram_description: "Wheel rotating about its center axis"

Example 8: "A flywheel spinning at 300 rpm is slowed down by a constant torque. It comes to rest after turning through 8 revolutions."
→ suggested_scenario: "rotational_kinematics"
→ knowns: {"omega": "300 rpm", "delta_theta": "8 revolutions"}
→ unknowns: ["alpha"]
→ diagram_description: "Flywheel labeled with 300 rpm, braking torque indicated"

Example 9: "A disk of radius 0.3 m and mass 2 kg experiences a torque of 4.0 N·m about its center. Find the angular acceleration."
→ suggested_scenario: "rotational_kinematics"
→ knowns: {"mass": "2 kg", "radius": "0.3 m", "torque": "4.0 N-m"}
→ unknowns: ["alpha"]
→ diagram_description: "Disk with radius labeled 0.3 m, torque arrow about center"

Output STRICT JSON ONLY. No markdown, no explanations, no conversational text, no "Answer:" prefix, no "The problem text is", no "**Known Variables**", no bullet points, no headings. Do not wrap in code fences. Start immediately with { and end with }. Your entire response must be valid JSON and nothing else. Do not include any text before or after the JSON.

EXAMPLE OUTPUT:
{"problem_text": "A ball is thrown at 20 m/s at 45 degrees", "knowns": {"v0": "20 m/s", "angle": "45 deg"}, "unknowns": ["range", "max_height"], "diagram_description": "Parabolic trajectory", "suggested_scenario": "projectile_motion"}

JSON schema:
{
  "problem_text": "string",
  "knowns": {"variable_name": "value with units"},
  "unknowns": ["variable_name"],
  "diagram_description": "string",
  "suggested_scenario": "projectile_motion|kinematics_1d|inclined_plane|atwood_machine|collision_1d|rotational_kinematics|mass_spring|energy_conservation|unknown"
}